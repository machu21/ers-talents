# Conventional Commit Workflow

When drafting commit messages:
1. Check `git status` and `git diff --staged`.
2. Follow Conventional Commits: `type(scope): concise description`.
3. Keep the first line under 72 characters.
4. Never commit sensitive keys, `.env*` files, or auto-generated Next.js marker files.